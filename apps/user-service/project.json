{
  "name": "user-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/user-service/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest build user-service"
      },
      "outputs": ["{workspaceRoot}/dist/apps/user-service"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start user-service --watch"
      }
    },
    "serve:prod": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "node dist/apps/user-service/main"
      }
    }
  },
  "tags": ["type:app", "scope:user"]
}
