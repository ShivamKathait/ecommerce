{
  "name": "admin-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/admin-service/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest build admin-service"
      },
      "outputs": ["{workspaceRoot}/dist/apps/admin-service"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start admin-service --watch"
      }
    },
    "serve:prod": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "node dist/apps/admin-service/main"
      }
    }
  },
  "tags": ["type:app", "scope:admin"]
}
