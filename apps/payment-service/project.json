{
  "name": "payment-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/payment-service/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest build payment-service"
      },
      "outputs": ["{workspaceRoot}/dist/apps/payment-service"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start payment-service --watch"
      }
    },
    "serve:prod": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "node dist/apps/payment-service/main"
      }
    }
  },
  "tags": ["type:app", "scope:payment"]
}
