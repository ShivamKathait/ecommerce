{
  "name": "inventory-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/inventory-service/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest build inventory-service"
      },
      "outputs": ["{workspaceRoot}/dist/apps/inventory-service"]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "nest start inventory-service --watch"
      }
    },
    "serve:prod": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "node dist/apps/inventory-service/main"
      }
    }
  },
  "tags": ["type:app", "scope:inventory"]
}
